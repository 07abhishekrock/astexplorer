
import React from 'react';

const RemoteEditor = ()=>{
  return <div>
    <h1>Websocket based remote editor</h1>
    <p>Start a local websocket server and push your code changes for this editor to subscribe.</p>
  </div>
}

export default RemoteEditor;
